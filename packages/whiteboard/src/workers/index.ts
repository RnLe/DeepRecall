/**
 * Workers Module - Worker Entrypoints (Placeholder)
 *
 * Future: Comlink-based workers for:
 * - Spatial index queries (R-tree in worker)
 * - WASM geometry operations
 * - Tile rendering with OffscreenCanvas
 */

// Placeholder - workers will be implemented when needed
export {};
