# Mobile App Production Environment Configuration
# Copy this file to .env.production and fill in your production values
# This file is used when running: pnpm run build:ios

# Electric Cloud (Production - Real-time Sync)
# Get from: https://console.electric-sql.com/
VITE_ELECTRIC_URL=https://deeprecall-production.up.railway.app/api/electric/v1/shape
VITE_ELECTRIC_SOURCE_ID=your-production-source-id-here
VITE_ELECTRIC_SOURCE_SECRET=your-production-secret-here
VITE_ENABLE_FOLDER_SOURCES_SYNC=false

# HTTP API for Write Buffer Flush (Production)
# This should point to your deployed Railway app
VITE_API_BASE_URL=https://your-app.railway.app

# Blob Storage Mode (Production)
# Options: 'native' (iOS filesystem) or 'cloudflare' (R2 cloud storage)
VITE_BLOB_STORAGE_MODE=native

# Optional: Cloudflare R2 Configuration (if using cloud storage)
# VITE_R2_ACCOUNT_ID=
# VITE_R2_BUCKET_NAME=
# VITE_R2_ACCESS_KEY_ID=
# VITE_R2_SECRET_ACCESS_KEY=

# Production Notes:
# - These values are bundled into the app at build time
# - Never commit this file to git (.env.production is gitignored)
# - For GitHub Actions, set these as repository secrets
# - Electric URL should be the full shape endpoint URL
# - API_BASE_URL must be HTTPS in production (Railway provides this)
