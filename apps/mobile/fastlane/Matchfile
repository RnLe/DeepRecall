git_url("https://github.com/RnLe/DeepRecall-certificates.git")

storage_mode("git")

type("appstore") # Changed to appstore (was development)

skip_certificate_verification(true)

app_identifier("com.renlehpy.deeprecall")
