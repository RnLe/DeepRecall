import type { Config } from "tailwindcss";

// Tailwind v4: sources are defined via @source in globals.css, plugins via @plugin
const config: Config = {
  theme: {
    extend: {
      // Add theme extensions here (colors, spacing, etc.)
    },
  },
};

export default config;
