/**
 * InputModal Wrapper (Next.js)
 *
 * Simple re-export of platform-agnostic component (no platform-specific logic needed)
 */

"use client";

export { InputModal } from "@deeprecall/ui";
export type { InputModalProps } from "@deeprecall/ui";
