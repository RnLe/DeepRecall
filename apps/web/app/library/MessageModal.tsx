/**
 * MessageModal Wrapper (Next.js)
 *
 * Simple re-export of platform-agnostic component (no platform-specific logic needed)
 */

"use client";

export { MessageModal } from "@deeprecall/ui";
export type { MessageModalProps } from "@deeprecall/ui";
