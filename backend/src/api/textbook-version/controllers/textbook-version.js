'use strict';

/**
 * textbook-version controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::textbook-version.textbook-version');
