'use strict';

/**
 * script-version controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::script-version.script-version');
