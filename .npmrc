# Use hoisted node_modules for native module compatibility and simpler Docker volumes
node-linker=hoisted

# Allow build scripts for packages that need native compilation
enable-pre-post-scripts=true

# Ensure consistent resolution across workspace
auto-install-peers=true
